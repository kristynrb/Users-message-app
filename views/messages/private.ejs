<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Omni Labs Interview - Private Messages</title>
</head>
<body>
  <h1><%= currentUser.username %>'s Private Message Page</h1>

  <h2>Write a private message</h2>
  <form action="/messages" method="POST">
    <input type="text" name="message" placeholder="message"/>
    <input type="hidden" name="author_id" value="<%= currentUser._id %>">
    <input type="hidden" name="status" value="private">
    <br>
    <input type="submit" value="submit" />
  </form>

  <hr>

  <h2>Private Messages:</h2>
  <!-- Message that there are "no private messages" if there are none. -->
  <ul>
    <% for(let i = 0; i < messages.length; i++){ %>
      <li>
        <%=messages[i].message%>
      </li>
      <% } %>
    </ul>
  </body>
  </html>
